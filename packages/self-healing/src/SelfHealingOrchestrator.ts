import { AgentVersionManager } from './AgentVersionManager';
import { IEventBus, AgentDriftDetectedEvent } from '@pamasmma/shared';
export class SelfHealingOrchestrator {constructor(private versionManager: AgentVersionManager, private eventBus: IEventBus) {} async handleDrift(agentId: string, driftScore: number) {const event: AgentDriftDetectedEvent = {type: 'agent.drift.detected', schema_version: 1, timestamp: new Date().toISOString(), tenant_id: 'system', task_id: 'self-healing', agent_id: agentId, drift_score: driftScore}; await this.eventBus.emit(event); const stable = this.versionManager.getStableVersion(agentId); if (stable) {console.log(`Rolling back agent ${agentId} to version ${stable.promptHash}`);} }}