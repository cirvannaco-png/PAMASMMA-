import { ToolRegistry } from '../domain/ToolRegistry';
import { InjectionDetector } from '../domain/InjectionDetector';
import { IMaliService, IMCPClient, IEventBus, AppError, ok, fail, Result } from '@pamasmma/shared';
export class ToolExecutionService {constructor(private registry: ToolRegistry, private injectionDetector: InjectionDetector, private maliService: IMaliService, private mcpClient: IMCPClient, private eventBus: IEventBus) {} async execute(toolName: string, inputs: unknown, context: { tenant_id: string; task_id: string; trace_id: string }): Promise<Result<unknown>> {const tool = this.registry.getTool(toolName); if (!tool) return fail(new AppError('Tool not found', 'TOOL_NOT_FOUND', 404)); if (this.injectionDetector.scan(inputs)) {return fail(new AppError('Injection detected', 'INJECTION_BLOCKED', 400));} if (tool.maliRequired) {const verdict = await this.maliService.evaluate(context.task_id, inputs); if (verdict === 'reject') {return fail(new AppError('Mali rejected execution', 'MALI_REJECTED', 403));}} try {const result = await this.mcpClient.call(tool.server, tool.toolName, inputs); return ok(result);} catch (err: any) {return fail(new AppError(err.message, 'MCP_CALL_FAILED', 502));}}}